Django Drop in Shop
===========

A fork of django-shop. I wanted a plugin to run a shop in django inside of an existing site. Django-shop was this for me, but the latest major update is no longer a plugin but a whole site.
